<Window
    x:Class="WpfDynamicGUI.DynamicFieldDemo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:WpfDynamicGUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfDynamicGUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="DynamicFieldDemo"
    Width="500"
    Height="350"
    MinWidth="500"
    MinHeight="350"
    MaxWidth="550"
    MaxHeight="350"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <ScrollViewer
            Grid.Row="1"
            Margin="5,0,0,0"
            VerticalScrollBarVisibility="Visible">
            <StackPanel>
                <ItemsControl Name="lstFunctions">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate />
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Label Height="3" />
                <Border BorderBrush="Black" BorderThickness="1" />
                <Label Height="3" />

                <DockPanel>
                    <Button
                        Width="100"
                        HorizontalAlignment="Left"
                        Click="OnAddFieldFunction"
                        Content="Weiteres Feld"
                        DockPanel.Dock="Left" />

                    <Button
                        Width="100"
                        HorizontalAlignment="Left"
                        Click="OnSave"
                        Content="Speichern"
                        DockPanel.Dock="Right" />

                    <Label Width="0" DockPanel.Dock="Right" />
                </DockPanel>
            </StackPanel>
        </ScrollViewer>

    </Grid>
</Window>
